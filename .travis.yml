sudo: false
language: node_js
node_js: lts/*
cache: npm
branches:
  only:
    - next
script:
  - npm start lint
  - npm start check-types
  - if [ "${TRAVIS_PULL_REQUEST}" = "false" ] && [ "${TRAVIS_BRANCH}" = "next" ]; then npm start test.ci; fi
  - if [ "${TRAVIS_PULL_REQUEST}" = "false" ] && [ "${TRAVIS_BRANCH}" = "next" ]; then npm start test.smoke; fi
  - npm start test
after_success:
  - npm start report-coverage
  # - npm start semantic-release
addons:
  sauce_connect: true
  chrome: stable
notifications:
  email: change
